/// `resp` defines names for actix-drop responses.
/// Currently, only HTTP responses are implemented.
pub mod resp;

/// `store` defines clipboard storage for actix-drop.
/// It currently supports unencrypted file and in-memory store.
pub mod store;

/// `config` stores
pub mod config;
